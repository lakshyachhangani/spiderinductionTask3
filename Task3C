n=int(input())
k=1
for i in range(3,n+1):              #In this we just have to find number of prime numbers in series because any other number would have a factor before it, so they will get same number
    for j in range(2,i):
        if i%j==0:
            break
    else:
        k=k+1                       #To count operation
print(k*(k+1)//2)                   # In this we are giving number to colour the first natural numbers because it's the way to find smallest sum
        
